// Define the variables
let num1 = 31;
let num2 = 2;

// Multiply num1 and num2 to get 62
let multiply = num1 * num2; // 31 * 2 = 62

// Generate a random integer greater than 0
let random = Math.floor(Math.random() * 100) + 1; // Ensures a positive integer

// Define variables for modulo operation
let num3 = 16;
let num4 = 6;

// Find the remainder of num3 divided by num4, which should be 4
let mod = num3 % num4; // 16 % 6 = 4

// Find the maximum number in a set (should return 20)
let max = Math.max(5, 10, 15, 20); // Returns 20
// Write your code here
